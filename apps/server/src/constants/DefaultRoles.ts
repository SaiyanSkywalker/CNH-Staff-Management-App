/**
 * File: DefaultRoles.ts
 * Purpose: Defines default roles to be added
 * to database if not already present
 */
const DefaultRoles = [{ name: "USER" }, { name: "ADMIN" }];
export default DefaultRoles;
