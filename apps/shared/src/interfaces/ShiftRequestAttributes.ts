/**
 * File: ShiftRequestAttributes.ts
 * Purpose: interface used to represent information for shift request
 */

export default interface ShiftRequestAttributes {
  user: string;
  shift: string;
  shiftDate: string;
}
