/**
 * File: UnitAttributes.ts
 * Purpose: interface that shows structure of info related 
 * to users and a socket
 */
export default interface UserSocketAttributes {
  username: string;
  uuid: string;
  isAdmin: boolean;
}
